/**
 * Collection Screen Component
 *
 * Serves as the container component for the collection view where users can
 * browse their collected stamps and postcards.
 *
 * Features:
 * - Renders CollectionView component
 * - Tab-based navigation integration
 * - Displays user's collected items
 *
 * @component
 */
import CollectionView from "@/components/views/CollectionView";

const Collection = () => {
  return <CollectionView />;
};

export default Collection;
