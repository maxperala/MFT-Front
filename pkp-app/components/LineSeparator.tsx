import { colors_new } from "@/colors";
import { View } from "tamagui";

const LineSeparator = () => {
  return (
    <View width="100%" height={1} backgroundColor={colors_new.dirty_white} />
  );
};

export default LineSeparator;
